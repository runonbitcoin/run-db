const { BaseTrustList } = require('./base-trust-list')
const { DbTrustList } = require('./db-trust-list')
const { TrustAllTrustList } = require('./trust-all-trust-list')

module.exports = {
  BaseTrustList,
  DbTrustList,
  TrustAllTrustList
}
